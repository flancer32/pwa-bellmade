/**
 * Plugin constants (hardcoded configuration) for shared code.
 */
export default class Bellmade_Shared_Defaults {
    NAME = '@flancer32/pwa-bellmade';

    constructor() {
        Object.freeze(this);
    }
}
